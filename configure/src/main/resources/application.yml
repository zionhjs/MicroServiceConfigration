# for .properties format
#my.greeting=Hello World
#
#my.list.values=One,Two,Three
#
#dbValues={connectionString:"http://", userName:"foo", passWord:"pass"}
#
#db.connection=${dbValues}
#db.host=127.0.0.1
#db.port=1200
#
#management.endpoints.web.exposure.include=*

#for .yaml format
app:
  name: MyApp
  decription: Welcome to ${app.name}

my:
  greeting: Hello World
  list:
      values: One,Two,Three

db:
  connection: connection-string-here
  host: 127.0.0.1
  port: 1200

management.endpoints.web.exposure.include: "*"

#spring.profiles.active: test

spring.cloud.config.uri: http://localhost:8888

server.application.name: spring-boot-config
